import java.util.ArrayList;

public class Individual extends Account {
    public Individual(User usr, boolean authenticationStatus, ArrayList<Insurance> insurances) {
        super(usr, authenticationStatus, insurances);
    }
}
